# Proyecto Base de React con TypeScript + Vite

¡Bienvenido! 👋 Este es un proyecto base creado con **React.js**, **TypeScript** y **Vite** para que puedas comenzar a desarrollar tus ejercicios de manera rápida y sencilla.


### ⚙️ Instalación

1.  **Descarga el archivo**: Haz clic en el botón de descarga para obtener el archivo `.zip` que contiene el código base.

2.  **Descomprime el archivo**: Una vez descargado, descomprime el archivo en la ubicación que prefieras de tu computadora. Esto creará una nueva carpeta con todos los archivos del proyecto. 

3.  **Abre la terminal, ventana de comandos, PowerShell de Windows, donde sea que desees ejecutar tus comandos**: Navega hasta el directorio del proyecto que acabas de descomprimir. Puedes hacerlo con el comando `cd`:

    ```bash
    cd ruta/a/tu/proyecto
    ```

4.  **Instala las dependencias**: Una vez dentro del directorio, ejecuta el siguiente comando para instalar todos los paquetes y dependencias necesarios que se encuentran en el `package.json`:

    ```bash
    npm install
    ```

    Este comando creará una carpeta `node_modules` en tu proyecto. 📦

---

### ▶️ Ejecutando el Proyecto

¡Ya casi terminas! Ahora que todo está instalado, puedes iniciar el servidor de desarrollo.

1.  **Inicia el servidor de desarrollo**: Para ver el proyecto en tu navegador y habilitar la recarga en caliente (hot-reloading), ejecuta:

    ```bash
    npm run dev
    ```

2.  **Abre tu navegador**: La terminal te mostrará una URL local (generalmente `http://localhost:5173`). Abre esa dirección en tu navegador y ¡listo! 🎉 Ya puedes empezar a codificar.

---

### 📜 Scripts Disponibles

En este proyecto, puedes utilizar los siguientes scripts:

* `npm run dev`: Inicia el servidor de desarrollo con Vite.
* `npm run build`: Compila el proyecto para producción en la carpeta `dist`.
* `npm run lint`: Ejecuta el linter de ESLint para revisar el código.
* `npm run preview`: Sirve localmente el contenido de la carpeta `dist` para previsualizar la versión de producción.